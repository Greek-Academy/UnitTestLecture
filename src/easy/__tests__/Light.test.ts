import { Light } from "../Light";
// TODO:必要なテストを記載してください
describe('Light class test...', () => {
	let light: Light;

	beforeEach(() => {
		light = new Light();
	});

	test('Lightの状態遷移のテスト', () => {

	});
});